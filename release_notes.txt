Database Changes for upgrading from 1.0 to 1.1

1. Must add the sc_field_dictionary table

2. Update drop_down_options related_field names
update sc_drop_down_options SET related_field_name = 'lead_source_id' where related_field_name = 'lead_source'
update sc_drop_down_options SET related_field_name = 'lead_status_id' where related_field_name = 'lead_status'
update sc_drop_down_options SET related_field_name = 'company_type' where related_field_name = 'account_type'

3. Must add the sc_module_relationships table

4. Drop the sc_modules table and re-create and insert.  Added view_layouts, icon and listview_layout.
Export structure/data and re-import for install

5. Language for a user
ALTER TABLE `sc_user_profiles`
	ADD COLUMN `language` VARCHAR(50) NOT NULL DEFAULT '0' AFTER `upro_last_name`;

New System Requirements
* PHP 7.1 or higher
* MySql 5.7 or higher
